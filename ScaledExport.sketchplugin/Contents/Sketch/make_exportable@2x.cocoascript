@import 'make_exportable.cocoascript'

var onRun = function(context) {
    makeAllExportable(context, 2.0);
    var doc = context.document;
    [doc showMessage:"Layers were made exportable for @2x project"];
}